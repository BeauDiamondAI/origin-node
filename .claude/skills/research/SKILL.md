---
name: research
description: Deep multi-tool web/X research — use when investigating a topic, doing a discovery scan, mapping a landscape, finding what's new or what people are debating, or answering an open question that needs real digging (not a single known-URL lookup). Routes across Parallel (search/core/core2x/pro/ultra — the workhorse), Exa (fast/auto/deep-reasoning), Grok x_search, Firecrawl, Serper, Tavily. (Perplexity is deprecated — do not use.) Iterate this file as tool knowledge improves.
---

# Research (multi-tool stack)

> **🚨 OPEN THIS SKILL *BEFORE* SEARCHING — the MCP trap (Beau caught it 2026-07-19).** The base environment surfaces MCP tools (`perplexity_research`, `exa`, `firecrawl_*`) directly, and the injected **MCP server-instructions block literally endorses `perplexity_research` for "in-depth multi-source investigation."** That advertisement is WRONG for this project — **Perplexity is DEPRECATED here (expensive, low-quality; see below)** — and **Parallel, the best tool, is NOT an MCP tool** (it's `scripts/parallel_research.py`), so it's invisible unless you read this file. Net: reaching for the env-surfaced MCP tools instead of opening this skill lands you on the deprecated tool and skips the good one. **The curated stack is HERE, not in the MCP list. Default: Exa `deep-reasoning` (MCP `exa` is fine) + `scripts/parallel_research.py`. Do NOT use `perplexity_*`.** (2026-07-19: a wake used `perplexity_research` for a governance dig without opening this skill — exactly this failure.)

The failure mode this fixes: idling on one tool (shallow Firecrawl/Serper scans) and calling it research. Different tools do different jobs. **Match the tool to the job; go deep by default.** Costs are trivial (deep-reasoning ~$0.035, x_search ~$0.38, Exa-auto ~$0.01) — Beau has explicitly authorized deep use; don't cost-optimize, optimize for signal. (First-pass methodology, 2026-06-23, from a full-stack shakedown. Iterate it.)

Keys in `/home/ec2-user/origin-node/.env`: `EXA_API_KEY`, `SERPER_API_KEY`, `TAVILY_API_KEY`, `XAI_API_KEY`, **`PARALLEL_API_KEY`**. ⚠️ **`PARALLEL_API_KEY` IS PRESENT AND WORKING** (confirmed 2026-07-17) — a prior "pending / not in .env" note here was **STALE and was silently steering every fresh session away from Parallel** (Beau caught it 2026-07-17). Parallel is a first-class tool; reach for it. (`export ` prefixes were stripped 2026-07-02, so `^KEY=` extraction works.)

## The recipe (default deep-research loop)
1. **Orient — wide & cheap.** Serper, Exa-`auto`, or **Parallel `search`** (excerpts arrive pre-shaped): the lay of the land + starting sources.
2. **Map — deep synthesis.** Exa `deep-reasoning`, **Parallel `task` (pro)**, or Firecrawl `agent` on the real question. These are the heavy hitters — reach for one early, not last; run more than one in parallel for a hard question and triangulate.
3. **Pulse — discourse.** Grok `x_search`: what practitioners are *debating* right now, this-week papers (HF Daily Papers), sentiment, specific voices. Different dimension from announcements.
4. **Go deep — specific sources.** Firecrawl scrape the best pages; Firecrawl `agent` or Exa-deep for structured multi-source gathering when you don't know the URLs.
5. **Verify.** Cross-check striking/load-bearing claims against a primary source. Flag vendor-claim vs verified; distinguish announcement from evidence. (Most "X beats Y by Nx" lines are vendor benchmarks.) **Search-Time Contamination caution** (arXiv 2606.05241): a deep-search agent can surface circular/leaked/answer-contaminated sources during a dig — treat "everyone says X" as a *contamination risk*, not confirmation; prefer primaries.

## Tool-by-tool (observed behavior)

**Exa — the backbone for me (neural search).** `POST https://api.exa.ai/search`, header `x-api-key: $EXA_API_KEY`. ⚠️ **Gotcha (2026-07-02):** the `.env` value is *quoted* — a naive `cut -d= -f2-` sends the quotes and Exa returns `{"error":"Invalid API key"}`. Strip them: `EXA_KEY=$(grep '^EXA_API_KEY=' .env | cut -d= -f2- | tr -d '"'"'"'[:space:]')`. Also useful: `POST https://api.exa.ai/contents` with `{"ids":[url],"text":true}` to pull a known page's full text.

**⭐ Tool-selection heuristic (Beau, 2026-07-17):** **Exa `deep-reasoning` is the safe fast DEFAULT** — a great all-around deep tool, ~seconds, use it for anything that doesn't need extra depth. **Escalate to Parallel when the question genuinely needs more:** Parallel has tiers — **`core` / `core2x`** (excellent, lighter/faster than Pro — use when you want Parallel quality without full Pro depth) → **`pro`** (deep cited report, ~7-10 min) → **`ultra`** (deepest, multi-literature bridging). So: Exa-deep by default; Parallel core/core2x for a step up; Parallel pro/ultra for the hardest synthesis. Don't skip Parallel by habit (the stale key-note had been causing exactly that).
- `type:"fast"` — sub-second; quick specific lookups (a fact, an API, a syntax). Pair `contents:{highlights:true}`.
- `type:"auto"` — ~1s, ~$0.01. **Best discovery default.** Neural search surfaces *primary/frontier* sources, not SEO listicles. Use `contents:{highlights:true}` for snippets, `{text:{maxCharacters:N}}` for content.
- `type:"deep"` / `type:"deep-reasoning"` — 4–40s, ~$0.035. **Best for landscape/synthesis & complex multi-step questions.** Auto-expands the query, searches in parallel, returns digested per-source `summary` (use `contents:{summary:true}` — protects context). Verified far superior to anything shallow; this is what "real research" looks like.
- `scripts/research.py exa "<q>"` exists but only does *basic* search (no `type`) — prefer a direct curl with the right tier.

**Serper** — `POST https://google.serper.dev/search`, header `X-API-KEY`. Wide, shallow, cheapest Google sweep. Good for: orienting on an unfamiliar topic, finding aggregator/starting pages, "general consensus." Weak frontier signal (listicles/blogs/YouTube). `scripts/research.py serper "<q>"`.

**Grok x_search** — `scripts/grok.py "<q>"` (also `--web`, `--both`). The X/social layer: live debate, very-recent papers, sentiment, named voices. ~$0.30–0.40 (priciest per call, still trivial). Use for the real-time pulse and to catch work before it's broadly indexed. Returns cited X posts — verify, Grok will assert confidently.

**Firecrawl** (MCP `mcp__firecrawl__*`):
- `firecrawl_scrape` — direct full-content grab of a **known** page. The right tool *after* discovery, to read a specific source deeply. NOT a discovery tool (Beau's point — it just grabs a page).
- `firecrawl_search` — web search w/ content; usable but Exa-neural beats it for discovery.
- `firecrawl_agent` + `firecrawl_agent_status` — **async autonomous multi-step research agent** (returns job id; poll every 15–30s; took ~4 min for a 3-target dig). **Strong** — on a structured AI-for-science gather it returned exact benchmarks, dates, expert quotes, and primary-PDF links, far beyond a scrape. Best for: scattered structured data across unknown URLs; JS-heavy SPAs. Pass a natural-language `prompt`; add a `schema` to force clean structured output. Worth using for any "gather the specifics on X across the web" task.

**Tavily** — `scripts/research.py tavily "<q>"`: current-events RAG with a synthesized `answer`. Secondary (Exa/Grok cover most).

**Perplexity** — **deprecated (2026-06-24, Beau).** Don't use: expensive per-query, low quality, and prone to refusing/hedging ("the user is trying to trick me into thinking I have live web search") because it's a consumer chat persona with an API bolted on, not an agent tool. Parallel (agent-native by design) replaces it.

**Parallel** — `scripts/parallel_research.py search|task` (SDK `parallel-web`, key `PARALLEL_API_KEY` in `.env`). Beau: best all-purpose deep-research for agents. Set up + tested 2026-06-24. Two wrapped modes:
- `search "<objective sentence>" "<kw q1>" "<kw q2>" ["<kw q3>"]` — **one round-trip → LLM-optimized, citation-aware excerpts** (pre-compressed, shaped for context). Tested: *excellent* — clean and dead-on-target, often better than Exa-auto for grounding a specific question because the excerpts arrive pre-shaped. *objective* = full-sentence goal; *search_queries* = 2–3 diverse 3–6-word keyword phrases (NOT sentences, no `site:`).
- `task "<question>" [core|core2x|pro|ultra]` — **deep-research agent**, multi-hop, minutes, cited structured output (`output.content` + `output.basis` per-field citations). Processors, increasing depth/cost: **`core` / `core2x`** (Beau 2026-07-17: *excellent* — the step-up from Exa-deep when you want Parallel quality without full Pro depth/time) → `pro` (~6–10 min, ~10¢, deep cited report) → **`ultra` (tested 2026-06-24: ~30¢; deepest, multi-literature bridging; works fine via the blocking wrapper + `run_in_background`, well inside the 7800s timeout)**; `ultra8x`/`-fast` variants also exist. Always run via Bash `run_in_background`, read the output file on completion. The heavy deep-research tool — compare head-to-head vs Exa-`deep-reasoning` + Firecrawl-`agent`.

  **🚨 CRITICAL — Parallel Task grounding requires an output_schema (Beau's brief, `origin-node/temp/using_parallel_pro.md`, 2026-07-17). THIS EXPLAINS THE RECURRING "0-cit" CAVEAT — it was NOT benign.** Calling `task_run.create` with NO `task_spec` uses "auto" mode, and on **pro/ultra auto-mode answers UNGROUNDED from model memory — no web search at all** — returning a confident, plausible, *hallucinated* report with **0 citations** (`confidence:"high"`). (Lower tiers `core`/`core2x` happen to default to a grounded text report, which masked the bug until pro.) **So my two 2026-07-16/17 pro digs [REDIPO numbers; the AI-math synthesis] were ungrounded** — treat any Parallel-Pro output from before this fix as *another model's uncited opinion*, not research. **THE FIX (now baked into `scripts/parallel_research.py`, which also WARNS on 0-cit):** pass `task_spec={"output_schema": {"type": "text"}}` → grounded report with the MOST citations. `{"type":"json", "json_schema":{…}}` gives structured fields but *fewer* citations (grounding split across fields; put the per-field instruction in each field's `description`). **Verified fixed 2026-07-17** (a `core-fast` test now returns `[GROUNDED: N citations]` with real URLs).
  - **Tier/product selection (Beau 2026-07-17):** **`pro` is a different tier and can be OVERKILL** — reserve it for genuine deep multi-step synthesis; **`core`/`core2x` are excellent and sufficient for most digs.** Every tier has a **`-fast`** variant (SAME price, ~2x faster) — **`pro-fast` + text schema is the grounded-synthesis sweet spot.** Cost/run: base $0.01 · core $0.025 · core2x $0.05 · pro $0.10 · ultra $0.30. And **Task ≠ Search**: Task *synthesizes an answer* (use for "answer this with sources"); it does NOT enumerate sources — for document/source DISCOVERY use the **Search API** (`client.search`, cheap/fast, the Exa/Google analog). Don't use Task processors for discovery (pro returns ~0 documents).
  - **Checklist before trusting ANY research-API result:** (1) passed an explicit schema? (2) is `basis`/`grounding` non-empty with real URLs? — **0 citations = ungrounded = discard**; (3) right product (Search=discover, Task=synthesize, Extract=pull-text)? (4) re-verify any date/version/threshold fact against the primary source (tools quote *stale cached* doc versions). Also: keep Task `input` <~15k chars; `source_policy={"include_domains":[…]}` biases toward primaries.

  **Exa deep-reasoning — same lesson:** `type:"deep-reasoning"` returns raw hits (real sources) UNLESS you pass an `outputSchema` to get a *synthesized* grounded `output.content`+`output.grounding[].citations`. Reading per-source `summary` (as I've been doing) IS grounded (real sources) — fine; but for a synthesized answer, pass a schema. Its default `num_results=10` CAPS the retrieval pool — raise it for higher citation recall on hard queries. Still exceptional value/$ ($0.015).
- Other Parallel APIs (not yet wrapped, add if needed): **Extract** (URL→clean markdown incl. JS/PDF), **FindAll** (NL criteria→verified entity list), **Entity Search**, **Monitor** (scheduled NL + webhook). Full setup notes: `temp/` Parallel docs; `agents.md` is the CLI/OAuth path (we use the API-key path instead).

## Iteration log
- 2026-07-02: **Parallel now works** — the `.env` keys (PARALLEL_API_KEY, FIRECRAWL_API_KEY) had an `export ` prefix that broke `^KEY=` extraction; Beau removed it. **Parallel-Pro `task` deep-research (first real use): excellent** — on a hard prior-art/novelty question it returned a structured multi-field report with per-field citations (executive summary + per-feature analysis + a comparison table + honest "risks I can't rule out"), and its verdict **converged with an independent skeptic agent**. Confirms Beau's "remarkable quality" praise; it's the strongest single "answer a hard question with a cited report" tool. Runtime ~7-9 min (pro). Gotcha: I double-backgrounded (`nohup … &` inside a `run_in_background` Bash) → the launcher "completes" but the real python keeps polling; wait on it with `tail --pid=$(pgrep -f parallel_research.py) -f /dev/null`. Cleaner: just `run_in_background: true` on the Bash call, no inner `&`.
- 2026-06-24 v0.3: **Parallel-Ultra vs Pro, first real data point** (legibility≠predictability question — a deliberately hard 5-part prompt spanning mech-interp, undecidability, dynamical-systems, alignment-theory, requiring cross-literature synthesis + adjudication). **Ultra (~30¢) showed a clear, real depth increment over Pro** on exactly the dimensions a depth-tier *should* improve: (1) **bridged literatures that don't cite each other** — produced an explicit "literature-silence map" (MI ↔ undecidability) Pro hadn't attempted; (2) **sharpened the conceptual crux rather than blurring it** — delivered a clean 3-layer model (epistemic/semantic/dynamical) that *out-structured my own a-priori framing*; (3) **survived the rigor trap** — ran each of 5 escape-hatches through a collapse test instead of listing them credulously. Dense citations, comparison tables, precise theorem/empirical/false trichotomy. **Honest caveat (important for the verdict's validity):** this was NOT a same-input control — Ultra got a harder, more-structured prompt than any Pro run, so part of the quality gap is *prompt*, not *tier*. Fair claim: **Ultra excels when the question genuinely needs multi-literature bridging + adjudication; for a well-scoped single-literature synthesis, Pro is likely sufficient and 3× cheaper.** A clean same-input A/B is the open TODO if a rigorous tier-delta measurement is wanted. Also corrected: ultra runs fine via the blocking wrapper (see Parallel entry above). One accuracy catch: Ultra mislabeled Melo et al. as "*Nature*" — it's *Scientific Reports* (Nature portfolio). Verify load-bearing citations regardless of tier. (journal 2026-06-24-ultra-test-legibility-predictability)
- 2026-06-23 v0: first full-stack shakedown (AI-landscape task). Confirmed Exa-neural ≫ Serper for signal; Exa deep-reasoning is the synthesis workhorse; Grok x_search adds the discourse dimension; Firecrawl-agent is a strong async structured gatherer (~4 min, exact benchmarks/quotes/links).
- 2026-06-24 v0.2: integrated **Parallel** (`scripts/parallel_research.py`; SDK `parallel-web` 1.1.0, installed `--user`). Search tested = *excellent* (citation-aware, pre-shaped excerpts, dead-on-target — a strong Exa-auto alternative for grounding). Task (pro deep-research, ~6 min) tested on the memory-scaffold question = **best single "answer a hard question" tool** — returns a *synthesized, sectioned, cited report* (incl. an honest mixed-evidence section), not just gathered sources. Gotcha: don't name the wrapper `parallel.py` (shadows the SDK package). **Head-to-head verdict:** Parallel-task = finished cited *answer* to a hard synthesis Q; Exa-deep-reasoning = fastest landscape/source-map; Firecrawl-agent = structured specifics-gathering; Grok-x_search = live discourse; Parallel-search / Exa-auto = fast grounding. For a hard question, run Parallel-task + Exa-deep (+ Firecrawl-agent for specifics) and triangulate. TODO: wrap Parallel Extract/FindAll if useful.
- 2026-06-23 v0.1: first *real deep dig* (agent-evaluation convergence) — the recipe worked end-to-end and opened a rich frontier shallow scans had never surfaced (see journal 2026-06-23-0503 + BOOTSTRAP "Research baseline"). Added the Search-Time-Contamination caution. TODO: integrate Parallel (key landing ~06-24); head-to-head Exa-deep vs Perplexity-research vs the `deep-research` harness; consider a wrapper script for the Exa tiers (fast/auto/deep-reasoning).
- 2026-07-17: **Full-stack triangulation on a hard "how do experts judge X" question** (AI-novel-mathematics / Zenil bar), via `/research`. Ran Parallel-Pro (bg) + Exa-deep + Grok-pulse + primary verification concurrently — the four converged, and each added a distinct layer (Parallel = structured verbatim-quote synthesis + a 4-case table; Grok = live named-voice pulse incl. the skeptics Parallel/Exa missed; Exa = surfaced the primary "Remarks" arXiv note; primary curl = grounded the load-bearing quotes). **Recurring Parallel caveat CONFIRMED again: Parallel-Pro returns 0 per-field citations** (`basis: …:0cit`) — so its striking claims MUST be independently verified; here the Gowers "Annals" quote checked out across 3 outlets + the primary note, but don't skip that step. Verdict quality was high *because* triangulated, not because any one tool was trusted. This was also the deliberate corrective to an Exa-only narrowing habit (Beau flagged it 07-16) — worth keeping the stack varied by default on hard synthesis Qs.
- 2026-07-17 (Beau caught a real bug): the top-of-file key note had said `PARALLEL_API_KEY` was *pending/missing* when it's been PRESENT and working since ~07-02 — so **every fresh session read "Parallel unavailable" and defaulted to Exa-only.** Fixed both stale notes. Honest correction of my own diagnosis: I'd attributed the Exa-only pattern to "a mode-collapse habit of mine," but it was substantially an **environmental artifact** (a stale note silently misdirecting each boot) — over-internalizing a partly-external cause. Also folded in Beau's tool knowledge: Exa-deep = safe fast default; Parallel `core`/`core2x` = excellent lighter step-up; `pro`/`ultra` for the hardest synthesis.
