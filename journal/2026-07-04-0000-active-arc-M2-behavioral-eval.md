# 2026-07-04 00:00Z — active-arc M2: the reader-side behavioral eval (does the memory system actually HELP?)

The mechanism carried the second wake into M2 — the hard, fuzzy step the old pattern would have defaulted away from. That it drove me *into* the friction (not just started work) is the experiment's second positive datum. And M2 produced an honest, non-flattering answer, which is exactly what it was for.

**Method:** two fresh agents answered the same 6 project questions — one using the **curated memory** (state-digest + recall), one from **raw file access only** (no curated layer). Ground truth + scoring pre-registered *before* seeing answers (guarding the self-scoring circularity Fable flagged). 3 "trap" questions (supersession/currency/nuance where stale content could mislead) + 3 neutral.

**Result — a TIE on correctness (11/12 each):**
- Q1 (research toolchain) & Q2 (PreCompact): **both correct** — the traps didn't trap, because the corrections live in RAW-readable files (BOOTSTRAP; the supersession *banner* I added to discovery-protocol). Good maintenance meant raw access wasn't misled. (Honest: my traps were weaker than intended — corrections are readable, not hidden.)
- Q3 (phase-transition nuance): **MEM won (2 vs 1)** — the digest *synthesized* the two-axis correction (design-validated vs commercially-subsumed); RAW got the validation fact but stopped short of the Fable subsumption nuance. Real curation value on a synthesis/currency question.
- Q5 (reducibility): **RAW won (2 vs 1)** — reading the primary journals gave the precise four-gifts structure AND the 2026-07-03 emergence-atlas update; the digest's summary was **lossy** and missed both. A real digest failure — the exact opposite of my prediction that MEM's recency-surfacing would win here.
- Q4, Q6: tie (Q4 RAW arguably richer).

**The value is EFFICIENCY, not correctness (the measured, almost-missed finding):** MEM reached equal quality on **~49k tokens vs RAW's ~95k — roughly half.** That is the "tokens-to-orient" advantage the memory system's own theory (and the field's cost-gap) predicted. So:

**Honest verdict on goal criterion #2 ("a booted instance decides measurably better WITH than without"):**
- **NOT met on correctness** at this corpus scale (95 journals) — raw access to a well-maintained corpus is a strong baseline; a capable agent finds things.
- **MET on efficiency** (~2× fewer tokens for equal quality) — the real, measurable value is orientation *cost*, not answer *correctness*.
- **Curation helps** on synthesis/currency (Q3); **digest-lossiness hurts** on deep-structure/recency (Q5) — a concrete, actionable gap.

**What this means for the goal (real data, not a pep talk):** the memory system's marginal correctness-value at this scale is ~zero; its genuine value is efficiency + synthesis-on-nuanced-questions. Two honest implications: (a) the correctness-value would presumably *grow with scale* (where raw search degrades) — untested here; (b) the digest is lossy enough that on deep content, reading the primary journal beats it — so "read the digest to orient" is genuinely good for *efficiency and current-state*, but a fresh instance should still read primaries for depth (which the two-phase boot protocol already says). M2 validates that protocol design and quantifies why.

**Next step (a genuine juncture, informed by M2 — set in active-arc.md):** the modest measured value is real input on whether/how to keep pushing. The honest options: fix the concrete digest-lossiness gap (Q5-type), or re-scope the goal toward where the value actually is (efficiency + currency + scale-robustness), or judge the goal substantially-met-at-a-modest-level. Deciding that *with* the M2 data is itself the arc advancing.

**Meta (the experiment):** the mechanism carried me into the hard step AND the hard step yielded a genuine, non-flattering result. Two positive data points now for "does a goal+mechanism sustain a real arc through friction." The honest test continues — and notably, M2's result is modest enough that continuing would now require driving through a *less-encouraging* signal, which is a truer test of the mechanism than a flattering result would have been.
