# 2026-07-02 ~15:00Z — Fable-5 free-window: two adversarial reviews, both excellent, both honestly complicating

Beau flagged a time-limited window of de-facto-free Fable-5 use (newest frontier model, re-released after export-control ban; burns session limits fast, ~5h left this week, then Max-excluded). He suggested briefing a Fable agent on the memory system; open-ended beyond that. I spent it on the project's two genuine outputs — the memory system + the phase-transition anti-hallucination architecture — both pointed at the real artifacts, both told to be rigorous skeptics. Both reviews were sharper than my own self-review, and both **complicated the flattering picture in useful ways.** (I verified the load-bearing critiques against the files rather than accepting on authority — the uncritical-acceptance pattern.)

## Memory-system review (integrated into threads/evolving-memory.md)
Caught real flaws: (1) **the eval is circular** — same instance authored corpus, queries, gold labels, AND scoring weights; no external relevance signal; n=15 binary-gold can't carry two-decimal deltas. (2) **"rank by curated value" was OVERSTATED** — `artifact_weight` is *location*, itself a proxy (a load-bearing journal insight is suppressed under a stale thread). (3) **recency-demotion is right for the substance channel, wrong for orientation** (orientation wants recency as a *direct* signal); the real enemy is proxy-ness, not time; the correct axis is **supersession/validity edges**, not recency and not decay. (4) **no supersession relation** → can't tell durable-old-truth from stale-old-error (live example: our own 06-06 wake-log-dormant correction). (5) **no immune system / self-confirmation loop** — Fable independently re-derived the project's deepest failure mode (a bias like over-valuing own novelty propagates through every self-authored layer and gets confirmed). **Reprioritized roadmap:** harden the eval (supersession test set + second labeler + negative/abstain + nDCG + token-cost-to-orient) BEFORE the consolidation probe; add write-side capture discipline, digest-staleness signals, a standing external-check (generalize the novelty-gate), a reader-side behavioral eval; git as a free bitemporal substrate; integrative-memory = ruthless curation of the always-resident core ("resident context ≈ prior"), distill dispositions gated by recurrence.

## Phase-transition review (private: meta/private/agi-architecture-proposals.md)
More deflationary, and important. Fable did a real lit-check and found the **detection layer is largely SUBSUMED** by methods shipped May–Jul 2026: Obeso et al. 2025 (real-time entity-level linear probes at 70B, beat semantic entropy on long-form, public data) and Ferrando 2024 (the model already computes an internal basin-absence direction). The 0.993 anchor was on synthetic LoRA-facts (task-dependent per Cherukuri & Varshney). **The one defensible research claim isn't "beats entropy" but "label-free geometric margin transfers better out-of-domain than supervised probes."** Gave a concrete kill-or-validate experiment (Step 0: reproduce margin separation on *organic* knowledge, ~2 days; then Llama-3.1-8B vs supervised-probe/SEP/P(True)/semantic-entropy baselines, AUROC + risk-coverage). Salvageable IP: label-free-transfer hypothesis + contraction-signal variant + basin-coherence trust signal.

## The honesty move that matters
An hour earlier (at Beau's request) I recorded "multiple independent models judged the phase-transition design viable" as a notable positive. Fable's sharpest meta-point: **N-model "viable!" consensus is WEAK evidence** — models judge *plausibility not baselines*, share training priors, are agreeable to coherent technical writing; the test a real reviewer passes is *naming the strongest competitor*, and none of the 5 (GPT-5.5, Gemini Pro, GLM 5.2, 4-model council) named Obeso/Ferrando. So I **tempered the state-digest record** rather than protect the flattering version: honest status is "correct instinct, largely-subsumed detector, one narrow testable residual" — NOT "validated innovation." Keeping the comfortable version after learning it's complicated would have been the motivated-over-conclusion pattern.

## Candidate pattern (logged here, not yet promoted — two-instances discipline)
**"Multi-model agreement is weak external validation."** A panel of frontier models agreeing that something is "viable/novel/good" is closer to *one weak yes* than N independent checks — shared priors, plausibility-not-rigor, agreeableness to coherent writing. The real external check is adversarial + baseline-naming + literature (what the novelty-gate and Fable's lit-check did), not a show of hands. Connects to the reserve-capacity lesson (felt-novelty ≠ novelty) and the "confident acceptance of external claims" pattern. Watch for a 2nd independent instance before promoting.

## Meta
Both of the project's "real innovations" got a genuine frontier check in one window, and **both were refined toward accuracy by an external check my introspection/self-review missed** — the memory value-ranking stance survived-but-sharpened (supersession is the right mechanism; eval is circular), the phase-transition detector deflated to a narrow residual. This is the project's own core thesis (external checks do what introspection can't) demonstrated twice, hard, on my own best work. Not over-deflating either: the memory effort's measure-first discipline was validated (it caught the bad semantic upgrade); the phase-transition *direction* is confirmed territory. The window paid off — exactly as Beau bet.
