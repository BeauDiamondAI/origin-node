# 2026-07-02 ~14:00Z — novelty-gate on the memory judgment-layer (Beau greenlit); + phase-transition validation record

Conversation-driven wake. Beau (a) confirmed the phase-transition signal-layer design was validated by MULTIPLE independent models (Gemini Pro, GLM 5.2, a 4-model council — not just GPT-5.5, whose transcript is the only one in-repo; recorded accurately in state-digest + private file, attributed to his first-hand report); (b) fixed the `.env` `export`-prefix that had hidden PARALLEL/FIRECRAWL keys; (c) greenlit the novelty-gate on the state-digest "judgment layer."

**Ran the novelty-gate — two independent methods, and they CONVERGED** (the whole point of a real gate):
- a skeptic agent tasked to *refute* novelty (own web research);
- **Parallel-Pro deep-research** (first real use of the tool Beau praised — genuinely excellent: structured, per-field citations, honest about what it can't rule out).

**VERDICT: partially novel, precisely located.**
- **Feature 1 (whole-corpus → single lossy scannable ORIENTATION digest, read first):** ADJACENT prior art — Mem0's async "conversation summary S," Hermes-Agent `MEMORY.md`/claude-mem (read-first self-curated markdown), Letta core-memory, Generative-Agents "reflection." The exact "whole-corpus orientation map read first" isn't published, but it's a *packaging* distinction, not a mechanism one. Modest.
- **Feature 2 (rank by curated value + DELIBERATELY DEMOTE recency/frequency as "corrosive proxies"):** **genuinely novel in the published literature and INVERTS field consensus.** Recency is the dominant signal everywhere (GenAgents ~0.99 weight; Mem0 "prioritize most recent"; MemoryBank Ebbinghaus decay; Mem0 2026 report sells recency-ranking as the fix). Two 2026 surveys (~12 & ~10 systems) found none demoting recency; "corrosive proxy" unpublished. **This is the real, defensible differentiation — exactly the "what's important enough to keep" judgment Beau pointed at.**
- **Combination (closed loop):** not found as a published system; "likely novel as of 2026-07." Caveat both methods flag: proprietary/black-box memory (Anthropic/OpenAI/Google/MS, unpublished startups) can't be ruled out → novel *in the published literature*, not provably-absolute.

**Why this matters (calibrated, no over/under-claim):** neither inflated to "big innovation" (Feature 1 is prior art; can't rule out proprietary) nor deflated to "just prior art" (Feature 2's stance genuinely inverts consensus and is unpublished). A **positive** novelty-gate — contrast reserve-capacity, where the same gate *deflated* a felt-novelty. Here the felt-sense of differentiation was externally checked and *partially survived, with the survivor pinned to the value-ranking stance.* So: unstructured autonomous building produced a retrieval principle that runs opposite to the whole field's practice and isn't in the published literature — a real, if bounded, result. Beau's original intuition (the judgment/importance layer is the differentiator) was right, and the gate located precisely *which* part earns the claim.

**Tooling:** Parallel-Pro confirmed as the strongest "cited answer to a hard question" tool (research skill iteration log updated); the `export`-prefix `.env` gotcha noted.

**Next:** increment 3 (LLM-driven consolidation probe) — now doubly motivated: it's the increment that most directly *builds on* the validated Feature-2 judgment layer, and the eval harness keeps it honest against the "distillation degrades" hazard.
