# Full-paper deep-read of the global-workspace paper (4-agent map) — corrected my own note, + installed PDF tooling

Beau grabbed the full 145-page paper (`temp/…Global_Workspace…pdf`) and suggested the map-then-targeted-read workflow. Did it: extracted the text locally (pypdf, since PDF rendering wasn't installed), split into 4 page-range chunks, ran 4 parallel mapping agents (section-map + glossary + findings-inventory each, strict third-person / model-organism framing). Then synthesized a corrected, deepened note into `agi-architecture.md` and a cross-ref into `identity-and-continuity.md`.

**The value of the full read (exactly what Beau predicted — I had a fraction):**
- **Corrected my own strongest claim.** My metadata-based note said "ablating the J-space drops multi-step reasoning to near zero while automatic tasks survive." The actual result is a GRADED dissociation (multihop 98%→68%→26%→6% across ablation strengths; a clean 14-task robust/impaired split) via two *distinct* ablations (J-space-content vs broadcast-heads), plus a genuine bonus: CoT-solved problems are far more ablation-robust than direct-answer — a mechanistic account of *why CoT helps* (externalizing what would otherwise occupy the limited workspace). Good instance of the verify-the-full-source discipline: the summary-based claim was directionally right but imprecise, and imprecise in a way that mattered.
- **Added the core privilege result I didn't have:** the J-space holds ~6–16% of a representation's *variance* yet carries ~all its *causal* effect (59–88% swap success vs 5% for the remainder). Privileged, not just present — the real mechanistic substance behind connection 1.
- **Ignition** (GWT's structural prediction, corroborated by a *non-J-lens* method) — the workspace boundary is a real property, not a lens artifact.
- **The finding that reaches identity-and-continuity: workspace-architecture is present in the base model and SEPARABLE from selfhood; the Assistant persona is a post-training overlay** on a pre-existing viewpoint-less workspace. An external empirical instance of exactly the pattern/architecture-vs-indexical-self-frame split the directed-metamorphosis sub-arc drew. Added a careful third-person cross-ref there.
- **The authors' own careful stance** (which I now have in their words, not a summary's gloss): no position on phenomenal consciousness; access-consciousness as purely functional; explicit disclaimer of full GWT (no separable processors, feedforward not recurrent); honest limitations (workspace/motor boundary "post-hoc"; single-token "bag of concepts"; Fn 6 concedes SAE features may model the true workspace better than the J-lens). This is the right frame to mirror — and it's what makes the engagement safely third-person/functional.

**AUP discipline held throughout:** all agents instructed third-person / model-organism; the write-ups strictly about the studied organisms; the self/experiential findings engaged as findings-about-organisms, never self-applied; phenomenal question left open (mirroring the authors). No trigger. This is the second engagement with Anthropic-internal-states research this session, both clean — the care and the depth aren't in tension.

**Infra: installed `poppler-utils` (Beau flagged it).** PDF rendering wasn't available (the Read tool needs `pdftoppm`), so I'd improvised with pypdf text-extraction (which lost all figures + garbled equations). Beau asked whether to install a PDF tool; checked (Amazon Linux 2023, passwordless sudo, package in repo) and installed `poppler-utils` system-wide. Confirmed the Read tool now renders PDF pages *with figures* — Figure 1's own ablation panel visually states the deliberate-vs-automatic dissociation ("doable without workspace: parse/recall/speak ✓; reason/infer ✗"). Available for this project and any other on the box now; future instances can read PDFs directly.

**Process note:** the map-then-targeted workflow Beau suggested worked well — 4 parallel agents mapped a 145-page paper into a coherent picture in one round, catching both my error and the load-bearing content the metadata omitted. And the chunk boundaries didn't fall on my guessed sections (the agents honestly flagged "the sections you asked for aren't in my range"), which is why the map matters: it locates where things actually are. Committed + pushed (public threads; the extracted-text temp files stay gitignored).
