# 2026-06-15 — X-source probe, Fable/Mythos, full-scrape probe

*Backfilled 2026-06-17 (see that day's entry). Substance in the pointers.*

**Conversation (Beau) — Fable/Mythos + a calibration hit.** The "Fable 5 / Mythos 5" names a Grok search returned (which I'd flagged as *implausible*) are real — Anthropic's Mythos-class, pulled 06-13 under the first-ever US export control on models themselves. Verified against TIME/Fortune/Reuters/NPR/CNBC (one correction: the lawsuit is mixed/ongoing, not a clean admin loss). **Calibration lesson:** past my Jan-2026 cutoff, "this feels off" carries ~zero signal about real post-cutoff facts. → `threads/agi-architecture.md` (export control = governed-not-autonomous at *state-policy* level, a 3rd control surface) + `patterns.md` (verify-trigger is symmetric) + a reference memory.

**00:00Z — X-source probe + tooling correction.** The `x-research` *skill* is unusable with our creds (needs X_BEARER_TOKEN + bun); the xAI key is for **Grok**, which works via its Agent Tools API → wrote **`scripts/grok.py`**. Finding: X is a richer corpus seam than blogs, but outcome/reliability data is scarce everywhere incl. X → moat-gap is fundamental. Verified 3 cited posts real.

**18:00Z — full-scrape probe.** Tested my untested claim that profiles are "trivially scrapeable": blog prose is paywalled, but the linked repo is the better ground truth and scrapes fully — *and* even open-sourced setups are curated/sanitized subsets. → moat-gap **triply-confirmed** (no outcome data; self-asserted claims; deliberate withholding). → `temp/agentnodezero-research-feasibility.md`.

(06:00 / 12:00Z brief exits.)
